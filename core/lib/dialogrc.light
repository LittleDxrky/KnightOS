#
# Run-time configuration file for dialog
#
# Automatically generated by "dialog --create-rc <file>"
#
#
# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?,underline?,reverse?)

# Set aspect-ration.
aspect = 0

# Set separator (for multiple widgets output).
separate_widget = ""

# Set tab-length (for textbox tab-conversion).
tab_len = 0

# Make tab-traversal for checklist, etc., include the list.
visit_items = OFF

# Show scrollbar in dialog boxes?
use_scrollbar = OFF

# Shadow dialog boxes? This also turns on color.
use_shadow = ON

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (BLACK,WHITE,ON)

# Shadow color
shadow_color = (BLACK,BLACK,ON)

# Dialog box color
dialog_color = (WHITE,BLACK,OFF)

# Dialog box title color
title_color = (BLUE,WHITE,ON)

# Dialog box border color
border_color = (BLUE,WHITE,ON)

# Active button color
button_active_color = (RED,WHITE,ON)

# Inactive button color
button_inactive_color = (BLACK,WHITE,OFF)

# Active button key color
button_key_active_color = border_color

# Inactive button key color
button_key_inactive_color = (RED,WHITE,OFF)

# Active button label color
button_label_active_color = title_color

# Inactive button label color
button_label_inactive_color = (BLACK,WHITE,ON)

# Input box color
inputbox_color = button_inactive_color

# Input box border color
inputbox_border_color = button_inactive_color

# Search box color
searchbox_color = button_inactive_color

# Search box title color
searchbox_title_color = (BLUE,WHITE,ON)

# Search box border color
searchbox_border_color = (WHITE,WHITE,ON)

# File position indicator color
position_indicator_color = searchbox_title_color

# Menu box color
menubox_color = button_inactive_color

# Menu box border color
menubox_border_color = searchbox_border_color

# Item color
item_color = button_inactive_color

# Selected item color
item_selected_color = border_color

# Tag color
tag_color = searchbox_title_color

# Selected tag color
tag_selected_color = title_color

# Tag key color
tag_key_color = button_key_inactive_color

# Selected tag key color
tag_key_selected_color = (RED,BLUE,ON)

# Check box color
check_color = button_inactive_color

# Selected check box color
check_selected_color = border_color

# Up arrow color
uarrow_color = (GREEN,WHITE,ON)

# Down arrow color
darrow_color = uarrow_color

# Item help-text color
itemhelp_color = dialog_color

# Active form text color
form_active_text_color = border_color

# Form text color
form_text_color = (WHITE,CYAN,ON)

# Readonly form item color
form_item_readonly_color = (CYAN,WHITE,ON)

# Dialog box gauge color
gauge_color = searchbox_title_color

# Dialog box border2 color
border2_color = button_inactive_color

# Input box border2 color
inputbox_border2_color = button_inactive_color

# Search box border2 color
searchbox_border2_color = button_inactive_color

# Menu box border2 color
menubox_border2_color = button_inactive_color
